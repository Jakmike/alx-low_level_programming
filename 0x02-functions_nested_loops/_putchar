#include <unistd.h>

/**
*
* _putchan - writes the character c to stdiot
*@c: The character to print
*
*on error, -1 is returned, and errno is set appropriately
*/
int _putcha(char c)
{
	 return (write(1, &c, 1));
}
